# Food-11 data preview

This folder contains one representative image per Food-11 class so the dataset is visible in the GitHub/DagsHub Files view without committing the full 1.1 GB dataset.

The complete dataset remains versioned separately with DVC through `data.dvc`.
